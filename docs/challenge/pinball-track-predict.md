---
layout: challenge
id: apcs014
title: 彈珠台軌道預測
difficulty: hard
category: apcs
type: competition
tags:
  - 數學
  - 模擬
algorithm: pinball_track_predict
description: 預測第 I 顆彈珠落入哪一個袋子
params:
  t:
    type: int
    min: 1
    max: 1
testcase_plan:
  - literal: |
      5
      4 1
      4 2
      4 3
      4 8
      4 11
  - literal: |
      8
      2 1
      2 2
      3 1
      3 4
      5 16
      4 2
      2 7
      3 10
  - literal: |
      4
      6 32
      7 10
      8 100
      5 33
  - literal: |
      4
      10 300
      9 256
      12 50
      3 999
  - literal: |
      4
      14 1000
      11 777
      13 2048
      2 12345
  - literal: |
      3
      16 5000
      15 3000
      6 40000
  - literal: |
      3
      18 20000
      17 15000
      4 30000
  - literal: |
      3
      20 30000
      19 20000
      7 10000
  - literal: |
      3
      20 50000
      6 20
      3 20000
  - literal: |
      3
      20 25000
      18 25000
      5 15000
  - literal: |
      3
      20 50000
      3 2
      8 3000
  - literal: |
      3
      19 54000
      2 1
      9 1500
  - literal: |
      3
      20 45000
      16 6000
      2 49999
  - literal: |
      3
      20 35000
      13 15000
      4 40000
  - literal: |
      5
      2 9900000
      3 5100000
      4 5050000
      6 17
      20 1234
  - literal: |
      5
      3 7300000
      2 8800000
      4 4400000
      12 4095
      5 96
  - literal: |
      5
      4 5100000
      2 10000000
      3 5400000
      20 3000
      7 1000
  - literal: |
      5
      2 10000000
      4 5600000
      3 4800000
      9 255
      14 9000
  - literal: |
      5
      3 6600000
      4 6050000
      2 8100000
      11 700
      18 65535
  - literal: |
      5
      4 7200000
      3 5900000
      2 7400000
      16 12345
      6 31
generator: |
  import sys

  data = sys.stdin.read().split()
  T = int(data[0])
  out = []
  for i in range(T):
      D = int(data[1 + 2 * i])
      ball = int(data[2 + 2 * i])
      slot = 1
      for _ in range(D - 1):
          if ball % 2 == 1:
              slot = slot * 2
              ball = (ball + 1) // 2
          else:
              slot = slot * 2 + 1
              ball = ball // 2
      out.append(str(slot - (1 << (D - 1)) + 1))
  print("\n".join(out))
reference_solution: |
  import sys

  tokens = sys.stdin.read().split()
  count = int(tokens[0])
  results = []
  for i in range(count):
      levels = int(tokens[1 + 2 * i])
      span = 1 << (levels - 1)
      ball = (int(tokens[2 + 2 * i]) - 1) % span
      bag = 0
      for k in range(levels - 1):
          bag = bag * 2 + ((ball >> k) & 1)
      results.append(str(bag + 1))
  print("\n".join(results))
starter_code: ""
---
## 彈珠台軌道預測

夜市有一台彈珠機：上方只有一個投球口，下方是一排收集袋。整台機器由上往下共 `D` 層：

- **第 1 層到第 D−1 層是翻板**：第 1 層 1 片、第 2 層 2 片、第 3 層 4 片……第 `k` 層有 2^(k−1) 片。每一片翻板下面接兩條路，分別通往下一層相鄰的兩片翻板。
- **第 D 層是收集袋**：共 2^(D−1) 個，由左至右編號 `1, 2, 3, ...`。

翻板的規則：

1. 開機時，**每一片翻板都朝左**。
2. 彈珠落到一片翻板上時，會往這片翻板現在指的那一側滑下去。
3. 彈珠滑走的同時，這片翻板會被**扳向另一側**——下一顆經過這裡的球就會往反方向走。
4. 穿過第 `D−1` 層之後，彈珠就掉進它下方的那個袋子。

老闆一顆一顆地投球，第一顆、第二顆、第三顆……**球可以一直投下去，數量不必受袋子數限制**（袋子夠深，裝滿了也照收）。請寫程式預測**第 `I` 顆**球會掉進幾號袋子。

### 輸入說明

- 第一行：整數 `T`（`T >= 1`），表示這一筆要預測幾次
- 接下來 `T` 行，每行兩個整數 `D I`
  - `2 <= D <= 20`（機器層數；翻板有 `D − 1` 層，袋子有 2^(D−1) 個）
  - `1 <= I <= 10000000`（球號）
- 每次預測都是**重新開機**的機台（所有翻板回到朝左）

### 輸出說明

- 每行預測輸出一行整數：第 `I` 顆球掉進的袋子編號（介於 `1` 與 2^(D−1) 之間）

### 範例

**輸入：**

```
5
4 1
4 2
4 3
4 8
4 11
```

**輸出：**

```
1
5
3
8
3
```

範例說明：`D = 4` 的機台有 3 層翻板、8 個袋子。第 1 顆球一路照初始方向走，掉進 1 號袋；它把沿路的翻板都扳過去了，所以第 2 顆球在第一片翻板就往另一邊走，最後掉進 5 號袋。前 8 顆球依序掉進 1、5、3、7、2、6、4、8 號袋——正好每個袋子各一顆。第 11 顆球則掉進 3 號袋。

> 提醒：後段測資的球號會大到 10^7 的量級，而且同一筆裡有好幾次預測。經實測，「從第 1 顆球開始逐顆模擬、每顆球逐層往下走」的寫法——包含各種把迴圈壓短、把整段下降塞進同一行的精簡版——都會在這些測資上超出運算次數限制。請想想第 `I` 顆球的路徑能不能不靠前面每一顆球就直接推出來。
