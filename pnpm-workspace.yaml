# No dependencies currently require build-script approval (pnpm allowBuilds).
# wasm-pack is provided by cargo for local development and by
# jetli/wasm-pack-action in the release workflow — it is not an npm dependency.
