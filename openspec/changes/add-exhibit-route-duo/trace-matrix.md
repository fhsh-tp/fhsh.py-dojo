# 追溯矩陣（change C：add-exhibit-route-duo）

本檔為本 change 的**單一真相來源（SSOT）**。proposal／design／spec／題目頁面的每一句量化敘述都必須指回這裡的事實 ID；沒有 ID 的數字不得出現在任何文件。路線 ID 的機械正本是 `curation/plan013.py::CAPS` 與 `curation/plan014.py::CAPS`——本檔與 design 的路線清單必須與那兩個字典逐條對齊。

量測環境：node-Pyodide（`node_modules/pyodide`，與站台自架版本同源），harness＝`curation/probe.mjs`（複製 `worker-utils.ts::buildWrappedCode` 的 tracer 包裝）。牆鐘數字為 node 環境值，僅作**相對量級**參考；出貨前以瀏覽器 e2e（V 表）覆核。

---

## C 表：平台與判題機制

| ID | 事實 | 證據 |
|----|------|------|
| C1 | 每筆測資的 op 上限為 10,000,000 | `pyodide.worker.ts::DEFAULT_OP_LIMIT` |
| C2 | op 計數器＝`sys.settrace` 事件計數（call／line／return／exception 各記 1），內建函式的 C 層工作**不計數**；同一行塞多個敘述只算一個 line 事件 | `worker-utils.ts::buildWrappedCode` 的 `_tracer`；實測見 A3（C 層路線比正解便宜）與 B3（攤平寫法每球 1 op） |
| C3 | worker 端每筆測資的 5,000 ms 軟旗標**對同步 Python 不會觸發**：旗標是 `setTimeout`（macrotask），而 `await pyodide.runPythonAsync(...)` 回來後的 `clearTimeout` 走 microtask、必定先執行 → 同步跑再久也判不到 TLE。R1 瀏覽器實測：每筆約 9.7 秒的提交仍逐筆 AC、總分 20/20。專案既有紀錄（gem-blast RCA）已記過同一事實，本 change 初版誤把它當成有效閘門 | `pyodide.worker.ts::WALL_CLOCK_MS`／`wallClockTle`；R1 e2e 實測 |
| C4 | 主執行緒硬砍為 `測資數 × 6,000 ms` **累計**（20 筆＝120,000 ms），到期即 `stop()`：worker 被終止、分數以當下已回報的結果定案；未回報的測資不會產生列 | `useExecutor.ts::WALL_CLOCK_KILL_MS`、`totalBudget = testcases.length * WALL_CLOCK_KILL_MS` |
| C4b | C4 觸發時的畫面呈現：`ChallengeView` 的「得分：N / 總數」用的是 store 的總測資數（正確），但結果表格是以已回報列數為分母 → **中斷的執行可能在表格上看起來全綠**。本 change 以「陣亡提交的總牆鐘必須遠低於 120 s」規避（B7），不改引擎 | 設計期賞金 F17；`useExecutor.ts::stop()` 與 `TestResultPanel.vue` |
| C5 | 判定＝stdout 比對（兩側先 `trimEnd`，容忍尾端空白／換行後逐字相等）；未捕捉例外→RE；op 跳閘→TLE | `pyodide.worker.ts::computeVerdict`、`opLimitExceeded` |
| C6 | Pyodide `sys.getrecursionlimit()` = 1000；tracer 之下單框遞迴實測最深 994 層，超過拋 `RecursionError`（乾淨例外，非 worker 崩潰） | `curation/probe_depth.py`（二分量測，可用 `curation/probe.mjs` 重跑） |
| C7 | 單筆測資輸入硬上限 65,536 bytes 不可覆寫，`input_budget` 宣告值須小於此數；literal 條目以實際 byte 數計入 | `Usage.md`〈輸入規模預算〉 |
| C8 | `testcase_plan` 的 literal 內容參與正式池 seed 導出——改任何一字都會重洗整池 | `Usage.md`〈測資決定性〉 |
| C9 | 正式池存 `floor(200 ÷ 每場筆數)` 個 block；全 literal 計畫下各 block 內容相同 | `Usage.md`〈正式池結構〉 |
| C10 | 題目 `id` 由 `pnpm new-challenge` 配號，不得手填；`--category apcs` 決定前綴與上架頁 | `scripts/new-challenge.ts` |
| C12 | **op 計數可被提交端規避（平台層）**：計數器只在 `sys.settrace` 事件觸發，提交只要在開頭呼叫 `sys.settrace(None)` 就把計數凍結（R1 實測全場 5 ops），op 上限完全失效；此性質對全站所有 op 斷崖題（apcs005／007／008／010、gem-blast 等）一體適用，非本 change 可用測資修補 → 記錄並建議另開 change（例如改用 `sys.monitoring` 或在 wrapper 外層加封鎖） | R1 賞金 `audit_r1/A_untrace.py` 實測 |
| C13 | **同一行可塞任意多敘述**：tracer 每行只記一個事件，因此把 K 顆球的完整下降串到同一 source line 可讓成本降到「每 K 球 1 op」，任何以 op 數為門檻的斷崖都可被 K 稀釋；而 C3 軟旗標對同步碼無效、C4 只在整場超時才中斷 → 成本軸**沒有任何閘門守得住這條路線**（處置見 B3b） | R1 賞金 `curation/routes/r014_batch32.py` 實測 |
| C11 | 生產建置只剝除 `generator` 與 `reference_solution`，**`testcase_plan` 的 literal 內容會原樣進入前端 bundle**；本專案的 repo 亦為公開，出貨測資輸入本來就可見 → 全 literal 題目的「離線算好答案再硬編碼」是**專案層既存殘餘**（apcs006、apcs009–012 同此），非本 change 新增；處置見〈賞金結果 F18〉 | `openspec/specs/generator-strip-plugin/spec.md`（明列 `testcase_plan` 保持原樣）、`ChallengeView.vue` 讀取 frontmatter |

---

## A 表：apcs013 展場動線重建

| ID | 事實 | 證據 |
|----|------|------|
| A0 | 出貨實測（ship-measured，R2 資料）：`reference_solution` 最壞 58,276 ops／18 ms；`generator` 最壞 107,501 ops／30 ms；收編的 `.index()` 路線最壞 38,872 ops／18 ms；收編的鏡射解最壞 120,475 ops／42 ms。全部遠低於 C1 | `probe.mjs` 對 `curation/literals/c013_*.txt` 全 20 筆 |
| A1 | 語義：每筆測資含 T 組；每組為「模式標記 M＋展區數 n＋兩串打卡序」。模式 1＝甲（進區先打卡→左岔→右岔）＋乙（左岔→打卡→右岔）求丙（左岔→右岔→打卡）；模式 2＝乙＋丙求甲 | `curation/semantics013.py`（正本） |
| A2 | 正解（雜湊索引＋顯式堆疊，無遞迴）成本約 31 ops/展區、與形狀無關 | `probe.mjs`；探針 n=2000 三形狀同為 62,035 ops |
| A3 | `.index()`＋切片天真解重活全在 C 層（C2），成本反而比正解低（出貨最壞 25,036 ops，見 A0）→ **不可獵殺，收編為聰明解**。附註（探針、非出貨形狀）：輸入預算容許的極端 n=6000 左鏈為 60,024 ops／715 ms，該形狀同時違反 A5 深度上限與 A8 byte 上限，只作上界參考 | `probe.mjs` 對出貨 literal 與探針輸入 |
| A4 | 純 Python 逐格掃描版在 n=2000 左鏈為 4,028,025 ops、未跳閘；要超過 C1 需 n≈6000 的深鏈，而該深度違反 A5 → 本題**不建 op 斷崖**，鑑別軸為語義 | `probe.mjs sol/c013_naive_scan.py`（探針） |
| A5 | 出貨測資的動線深度上限訂為 **300**（實際最深＝300）：由 C6（994 層）推得，對每層 1 框的遞迴解有 3.3 倍餘裕。實證：不調 recursionlimit 的遞迴解在全 20 筆最壞 38,871 ops、無 RecursionError（含深度 300 的組別）| 設計決策＋`probe.mjs sol/c013_naive_norecur.py literals/c013_19.txt` |
| A6 | 路線清單與**出貨實測**得分（`plan013.py` 逐筆比對，非估算）：W1 模式盲 10／W5 只讀一次模式標記 11／W13 深度啟發式（完全不讀標記）11／W2 模式 2 取丙序首元素 10／W4 模式 2 展開順序誤為先左後右 10／W3 誤把編號排序當第二串 0／W6 只解第一組 2／W9 反轉第一串 0／W10 照抄第二串 0／W11 形狀字典（零重建）2／W12 形狀字典＋小 n 窮舉 2／Z1 照抄第一串 0／Z2 反轉第二串 0／Z3 降序排序 0；收編：R1 鏡射解 20、`.index()` 天真解 20 | `curation/plan013.py::CAPS`＋`report013.json` 的 `*_ok` 逐筆欄位 |
| A6b | W4 的精確語義＝「模式 2 自尾端消耗丙序時，展開順序誤為先左後右」。**全程一致地把左右改名是本題的對稱性、輸出與正解相同，屬收編、不得獵殺** | `semantics013.py::route_W4_swaporder` |
| A7 | 展區編號為 1..n 的**隨機重排**（非依中間序排序），封殺「把編號排序當第二串」的假設（W3＝0） | `semantics013.py::make_case` |
| A8 | 每筆測資 byte 上限 50,000、`input_budget` 宣告 63,488（< C7）；題面宣告 1 ≤ n ≤ 1500，出貨實際最大 n＝1400、最大筆 30,998 bytes | `curation/report013.json` |
| A9 | 每筆測資至少一組**左右結構不對稱**（鏡射後結構簽章改變），機械斷言已實作 | `plan013.py` 的 `mirror_asym` 檢查、`semantics013.py::mirror`／`sig` |
| A10 | 形狀多樣性以**結構簽章**判定（不是形狀名稱）：每筆至少兩種簽章、至少一組非單一路徑。必要性實證：`spine` 在 n ≤ 120 時 `rest = 0`、退化成純左鏈；`zigzag` 任何長度都是單一路徑 | `plan013.py` 的 `sigs`／`chain` 斷言、`semantics013.py::is_chain` |
| A11 | 三重交叉：語義正本的建構→三序輸出、`gen013.py`（顯式堆疊）、`ref013.py`（分段遞迴＋位置雜湊）三者在全 20 筆 literal 上輸出一致 | apply 期腳本比對，0 筆不符 |
| A12 | **收編路線 R1（鏡射解）**：模式 2 的答案＝reverse(模式1(reverse(第二串), reverse(第一串)))，語義等價、實測 20/20、最壞 77,609 ops。題面不得出現任何暗示「兩種模式必須各寫一套」的句子 | `semantics013.py::route_R1_mirror`、A0 |
| A13 | 輸入無二義性：標號兩兩相異時，(甲,乙)→丙 與 (乙,丙)→甲 皆為單射，n ≤ 11 窮舉 0 碰撞。模式標記必須存在的理由是**同一組輸入在兩種讀法下通常都合法但答案不同**（不是「另一種讀法會不合法」）——因此無法由資料自我推導，只能讀標記；能否用啟發式猜中則由 W13／A16 兩條上界約束 | 設計期賞金 F13（窮舉證明） |
| A14 | **模式 2 的左鏈組別對「模式盲」誤讀免疫**（左鏈的乙與丙相同，誤讀後恰好輸出正確的甲；n=1..9 窮舉：每個 n 恰一形狀免疫且為左鏈）。因此「每筆含模式 2 的測資至少要有一組非單一路徑、n ≥ 3」是 W1＝10/20 的真正守門機制，已寫成斷言 | 設計期賞金 F16；`plan013.py` 的模式 2 非鏈斷言 |
| A15 | 範例筆豁免：第 1、11 筆為題面兩個範例，單組、n=7，不承擔鑑別責任——W6（只解第一組）、W11（形狀字典）、W12（字典＋小 n 窮舉）各拿這 2 筆即為契約地板。其餘 18 筆的最大 n 一律 ≥ 20；**這條規則的作用不是「讓窮舉不可行」**（窮舉在 n=9 以下才受限），而是把小 n 組別排除在「唯一承擔鑑別責任的組別」之外——真正的封殺來自 W11／W12 兩條路線的 eq 2 契約，資料必須逐筆讓它們失敗 | `plan013.py::EXAMPLE_ENTRIES`、CAPS 的 W11／W12 |
| A16 | 「不逐組讀模式標記」的規則家族上界＝**12/20**：規則可用 (組數, 首個模式, 位置)、(組數, 首個模式, **組別大小名次**) 或 (組數) 任一種鍵；最佳規則只能命中同鍵之下出現最多次的那一種模式序列。出貨的 9 筆混合筆皆為 5 組、首個模式皆為 2、**組別一律依大小遞減排列**（讓大小名次鍵與位置鍵指向同一個桶）、模式序列兩兩相異 → 三種鍵的上界皆為 11 + 1。R2 指出只算位置鍵不夠：大小名次鍵在舊排列下可拿 18/20 | `plan013.py::POSITIONAL_RULE_CAP`、`report013.json::positional_rule_best` |
| A17 | 「完全不讀模式標記、兩種讀法都還原一次、選總深度較小者」的啟發式路線出貨實測 11/20（受 A16 同一條上界約束）：讓它失手的機制是「正確讀法反而是較深的那個」的組別（zigzag／鏈狀），第 12、13、15、20 筆各補一組 | `semantics013.py::route_W13_depthguess`、`plan013.py::CAPS` | `plan013.py::POSITIONAL_RULE_CAP` 與 `report013.json::positional_rule_best` |

---

## B 表：apcs014 彈珠台軌道預測

| ID | 事實 | 證據 |
|----|------|------|
| B1 | 語義：機台共 D 層——第 1..D−1 層是翻板（第 k 層 2^(k−1) 片，初始全部朝左），第 D 層是 2^(D−1) 個袋子（左至右編號 1 起）。彈珠依翻板方向落下並把該翻板扳向另一側；穿過第 D−1 層後入袋。輸出第 I 顆球的袋號 | `curation/semantics014.py`（正本） |
| B1b | **球數可超過袋數**：機台狀態以 2^(D−1) 顆球為週期，bag(D, I) = bag(D, ((I−1) mod 2^(D−1)) + 1)，對任意 I 良定義。值域取 1 ≤ I ≤ 10,000,000（B4）與 **2 ≤ D ≤ 17**（R2-2：層數上限決定全域最大週期 2^16；若放到 20，「鑑別寫死模數 2^18」需要一條 I > 262,144 的 D=20 測試，而那條會讓收編路線 P1 跳閘——兩者互斥）；題面不得再寫「每顆球落入相異袋子」 | `semantics014.py` 說明＋交叉驗證（D=2..11 連跑 3 個週期，0 筆不符） |
| B2 | 三獨立實作（O(D) 奇偶直推／逐球全模擬／(I−1) 反向讀取）交叉驗證覆蓋 D=2..11 的整個週期與跨週期值，並含 D 至 20、I 取 {1, 2, 2^(D−1)−2, 2^(D−1)−1, 2^(D−1), 5,000,000, 9,999,999} 的抽樣，0 筆不符。D=4 落袋順序＝1,5,3,7,2,6,4,8 | `curation/semantics014.py` 與 apply 期交叉驗證腳本 |
| B3 | **op 成本模型（兩輪修正後）**：op 計數器只數 line 事件（C2），因此逐球模擬的成本取決於「每球佔幾個 line 事件」：一般寫法 4.158 ops/step、單行 while 1.105 ops/step、把單顆球的整段下降攤平到一行 ≈ 1 op/球。門檻因此用**球數**（不是步數） | `probe.mjs` 對 `curation/routes/r014_naive.py`／`r014_lean.py`／`r014_flat.py` |
| B3b | **成本軸的已知殘餘（平台層，測資無法封殺）**：把 K 顆球的下降串到同一行後成本降為「每 K 球 1 op」（C13），加上 C3 軟旗標對同步碼無效、C4 只在整場超時才中斷，因此 K 球批次寫法可以通過殺手帶——R1 瀏覽器實測 K=32 版 20/20。更簡單的 `sys.settrace(None)`（C12）連 op 計數都直接凍結。兩者皆屬「規避判題器」而非「解出題目」，處置：記錄為殘餘＋建議另開引擎面 change，不以測資規避 | R1 賞金 `routes/r014_batch32.py`、`audit_r1/A_untrace.py`；瀏覽器 e2e |
| B4 | 舊值域（I ≤ 2^(D−1)）下 op 斷崖**數學上不可建**：單組球數上限 524,288 → 每球 1 op 的攤平版約 524K ops／組，需 19 組才過 C1；而收編的逐層計數解第 5 組就跳閘 → 區間空集合。移除該上限（B1b）後斷崖恢復：攤平版成本 ∝ 球數，收編路線成本 ∝ 2^(D−1)、與球數無關 | 設計期賞金；`probe.mjs` 實測 |
| B5 | 出貨階梯：第 1–15 筆總步數 ≤ 1,000,000（實際最大 860,000），任何逐球寫法皆可過；第 16–20 筆總球數 ≥ 15,000,000（實際最小 15,464,112；1.5 倍餘裕）、總步數 ≤ 70,000,000（實際最大 67,440,568）→ 每球至少一個 line 事件的逐球寫法 = 15/20 | `curation/report014.json` |
| B5b | 步數**不設下限**：C3 既然無效，把步數堆高只會拖長陣亡提交的等待、逼近 C4 的整場中斷（C4b），不會多殺任何路線 | 設計決策，依據 C3／C4b |
| B6 | 逐球寫法實測（成本閘 `cost_gate.mjs`，套用 C1 與 C4、逐字比對輸出）：一般寫法（全場 17,151 ms）、單行 while 精簡版（25,262 ms）、依 D 攤平單球版（29,014 ms）三者皆 15/20 | `curation/report_cost.json` |
| B7 | 陣亡／殘餘提交的總牆鐘實測：三種逐球寫法 17–29 秒、K 球批次殘餘 69,120 ms（佔 C4 的 120,000 ms 累計預算 58%），皆在預算內；成本閘另設 70% 警戒線 | `curation/report_cost.json` |
| B8b | P1 的「大整數位元場」寫法語義正確、實測 20/20、全場 18,064 ms（D 上限降到 17 後大幅改善；R1 資料下曾達 92 秒／77% 預算）——仍列為已知慢寫法並在成本閘豁免時間警戒線 | `routes/r014_periodic_bits.py`、`report_cost.json` |
| B8 | **收編路線**（語義正確、必須維持 20/20）與其守門斷言：L1 逐層計數與 P1 取週期再模擬，各取**最貴**寫法計算預算——L1 遞迴版實測約 13 ops/節點 → 每筆 Σ2^(D−1) ≤ 600,000（出貨最大 98,368）；P1 取「模擬整個週期建表再查」的寫法（成本 ∝ (D−1)×2^(D−1)、與球數無關）→ 每筆 op 模型 ≤ 8,000,000（出貨最大 6,469,478）。用 min(I, 週期) 建模會誤殺這種寫法（R2 的 ladder-inversion 發現） | `probe.mjs` 對 `routes/r014_levelwise{,_rec}.py`／`r014_periodic{,_bits}.py`；`plan014.py::COOPT_NODES_CAP`／`PERIOD_BALL_CAP` |
| B9 | 零洞察／誤解路線出貨實測：Z1 原樣輸出球號、Z2 固定 1、Z3 固定最大袋號、E1 不做反向、E2 0-based、E3 翻板編號、E4 奇偶相反、E5 多走一層、E6 只解第一組皆 0/20；Z4「球號大於門檻就丟最大袋」= 15/20（＝逐球模擬的地板，不高於誠實解） | `curation/plan014.py::CAPS`、`report014.json` |
| B9b | 結構斷言（防退化資料）：每筆 ≥2 組且無重複 (D, I)；每筆至少一組 I ≥ 2；每筆至少一組同時滿足 bag ∉ {I, 1, 2^(D−1), 週期內球號}；**殺手帶的每一條大球數測試（I > 100,000）逐條**不得 I ≡ 0 (mod 2^(D−1))、不得 bag = 2^(D−1)、不得 bag = 週期內球號；每筆殺手帶至少一條「大球數且週期 > 8」的測試 | `curation/plan014.py` 結構斷言區 |
| B9c | 固定模數族：對每個 2 的冪次 M，「大球數先用 M 化約再算」的路線必須被鑑別出來；**豁免門檻取自值域最大週期 2^(17−1)=65,536，而非出貨資料的最大週期**（R2-2：用出貨資料當門檻會把「剛好夠用但值域內錯誤」的模數放行，實測 M=512 曾拿 20/20）。出貨實測：M ≤ 32,768 全部 ≤ 18/20，M ≥ 65,536 為 20/20（數學上正確的過度化約，豁免） | `plan014.py` 的 `fixed_period_family` 斷言 |
| B10 | 出貨實測：`reference_solution` 最壞 146 ops、`generator` 最壞 234 ops（皆 O(D)）；每筆測資 ≤ 61 bytes，`input_budget` 沿用預設 4096 即足 | `probe.mjs`、`report014.json` |

---

## V 表：出貨後量測

「離線」欄＝CPython 逐筆比對（`plan013.py`／`plan014.py`），「成本閘」欄＝`cost_gate.mjs`（node-Pyodide＋C1 op 上限＋C4 累計預算；**不模擬 C3**，因為它對同步碼無效），「瀏覽器」欄＝dev 站 agent-browser 實際提交。

| ID | 路線 | 路線檔 | 題目 | 契約 | 離線 | 成本閘 | 瀏覽器 |
|----|------|--------|------|------|------|--------|--------|
| V1 | `reference_solution` | `ref013.py` | apcs013 | 20/20 | 20 ✓ | 20、最壞 58,276 ops ✓ | 20/20 ✓ |
| V2 | `generator` | `gen013.py` | apcs013 | 20/20 | 20 ✓ | 20、最壞 107,501 ops ✓ | — |
| V3 | `.index()` 天真解（收編） | `routes/r013_indexnaive.py` | apcs013 | 20/20 | 20 ✓ | 20、最壞 38,872 ops ✓ | 20/20 ✓ |
| V4 | R1 鏡射解（收編） | `routes/r013_r1_mirror.py` | apcs013 | 20/20 | 20 ✓ | 20、最壞 120,475 ops ✓ | 20/20 ✓ |
| V5 | W1 模式盲 | `routes/r013_w1_modeblind.py` | apcs013 | 10/20 | 10 ✓ | — | 10/20 ✓ |
| V6 | W5 只讀一次模式標記 | `routes/r013_w5_markeronce.py` | apcs013 | 11/20 | 11 ✓ | — | 11/20 ✓ |
| V7 | W2 丙序取首元素 | `routes/r013_w2_postfirst.py` | apcs013 | ≤ 10/20 | 10 ✓ | — | 10/20（前一版資料）|
| V8 | W4 展開順序顛倒 | `routes/r013_w4_swaporder.py` | apcs013 | ≤ 10/20 | 10 ✓ | — | 10/20（前一版資料）|
| V9 | W11 形狀字典（零重建） | `routes/r013_w11_shapelib.py` | apcs013 | 2/20 | 2 ✓ | — | 2/20 ✓ |
| V10 | W12 形狀字典＋小 n 窮舉 | `routes/r013_w12_dictbrute.py` | apcs013 | 2/20 | 2 ✓ | — | 2/20 ✓ |
| V10b | W13 深度啟發式（完全不讀模式標記） | `routes/r013_w13_depthguess.py` | apcs013 | ≤ 12/20 | 11 ✓ | — | 11/20 ✓ |
| V11 | 不逐組讀標記的規則家族上界（位置／大小名次／組數三種鍵） | 計算式（非單一檔案） | apcs013 | ≤ 12/20 | 12 ✓ | — | 不適用 |
| V12 | `reference_solution` | `ref014.py` | apcs014 | 20/20 | 20 ✓ | 20、最壞 146 ops ✓ | 20/20 ✓ |
| V13 | `generator` | `gen014.py` | apcs014 | 20/20 | 20 ✓ | 20、最壞 234 ops ✓ | — |
| V14 | 逐球模擬（一般寫法） | `routes/r014_naive.py` | apcs014 | 15/20 | — | 15、全場 17,151 ms ✓ | 15/20 ✓ |
| V15 | 逐球模擬（單行 while 精簡版） | `routes/r014_lean.py` | apcs014 | 15/20 | — | 15、全場 25,262 ms ✓ | — |
| V16 | 逐球模擬（依 D 攤平單球版） | `routes/r014_flat.py` | apcs014 | 15/20 | — | 15、全場 29,014 ms ✓ | — |
| V17 | L1 逐層計數（壓行寫法，收編） | `routes/r014_levelwise.py` | apcs014 | 20/20 | 20 ✓ | 20、全場 1,268 ms ✓ | — |
| V18 | L1 逐層計數（**遞迴寫法**，收編預算來源） | `routes/r014_levelwise_rec.py` | apcs014 | 20/20 | — | 20、全場 2,868 ms ✓ | 20/20 ✓ |
| V19 | P1 取週期再模擬（收編） | `routes/r014_periodic.py` | apcs014 | 20/20 | 20 ✓ | 20、全場 6,033 ms ✓ | 20/20 ✓ |
| V20 | P1 大整數位元場寫法（已知慢寫法） | `routes/r014_periodic_bits.py` | apcs014 | 20/20 | — | 20、全場 18,064 ms ✓ | — |
| V21 | **K=32 批次攤平（平台層殘餘）** | `routes/r014_batch32.py` | apcs014 | 20/20（殘餘） | — | 20、全場 69,120 ms（佔 C4 預算 58%）| 20/20 ✓ |
| V22 | Z 系列與 E 系列誤解路線 | `semantics014.ROUTES` | apcs014 | 皆 0/20（Z4 為 15/20）| ✓ | — | E1／Z1 前一版資料實測 0/20 |
| V22b | 小模數（mod 4）查表＋逐球模擬 | `routes/r014_smalloracle.py` | apcs014 | ≤ 15/20 | 15 ✓ | — | 15/20 ✓（R1 資料下曾 20/20）|
| V23 | content-regression（兩題） | — | 兩題 | 全數一致 | 19 passed ✓ | — | 不適用 |
| V24 | challenge-params 冒煙 | — | 兩題 | 通過 | 73 passed ✓ | — | 不適用 |


---

## 賞金結果（設計期，I-1；10 條 lens × 對抗驗證）

| ID | 攻擊家族／缺陷 | 處置 | 落點 |
|----|----------------|------|------|
| F1 | apcs014 逐球模擬可攤平到每球 1 op（op 下界比原記錄低 21 倍） | 修：成本模型改用球數（B3） | `plan014.py::KILL_BALL_MIN`、`routes/r014_flat.py` |
| F2 | 舊值域下「收編逐層計數 ⇒ op 斷崖不可建」的結構性矛盾 | 修：移除 I ≤ 2^(D−1) 上限（B1b／B4） | `semantics014.py`、題面值域 |
| F3 | I = 2^(D−1) 是排列不動點（bag = I），零洞察路線白拿殺手筆 | 修：加入 Z1/Z2/Z3 路線與不動點結構斷言（B9／B9b） | `plan014.py` |
| F4 | 「D 層翻板 vs 2^(D−1) 個袋子」物理敘述自相矛盾，使 E5 變成題面 bug | 修：改寫為「第 1..D−1 層翻板、第 D 層袋子」（B1） | 題面、spec、SSOT |
| F5 | 題面範例落在邊界筆，與 `print(I)`／多走一層兩種誤讀相容 | 修：第 1 筆加 (4,11) 並加斷言「範例筆必須鑑別 Z/E 家族」 | `plan014.py` entry 1 斷言 |
| F6 | 殺手筆兩組測資完全相同，memo 寫法可對半砍成本 | 修：同筆禁止重複 (D, I)（B9b） | `plan014.py` |
| F7 | E4／E5 的上界過鬆、E5 的「每筆至少一組 I≥2」前提未寫成斷言 | 修：兩者收緊為 =0/20＋加結構斷言 | `plan014.py::CAPS` |
| F8 | B2 交叉驗證未覆蓋出貨極值 | 修：擴充到 D 至 20、含跨週期與近上限抽樣（B2） | `semantics014.py` 驗證腳本 |
| F9 | 收編路線的餘裕用最精簡寫法計算（方向錯誤） | 修：改用**最貴**合理寫法（顯式 if/else，5 ops/內圈）推導上限（B8） | `plan014.py::LEVELWISE_OPS_CAP` |
| F10 | 形狀字典路線（零重建）在出貨資料上得 5/20，斷言牆無此路線 | 修：收進 ROUTES、上界 2/20（僅範例筆），並把 entry 2/3/7/12 拉大（A6／A15） | `semantics013.py::route_W11_shapelib` |
| F11 | A10 以形狀「名稱」判多樣性；`spine` 在 n ≤ 120 退化成純左鏈 | 修：改用結構簽章＋非鏈斷言（A10） | `plan013.py`、`semantics013.py::sig` |
| F12 | 退化公式家族只守 4 選 2（缺照抄第一串／反轉第二串／降序） | 修：補 Z1/Z2/Z3 路線，上界皆 0/20（A6） | `semantics013.py` |
| F13 | 輸入無二義性（窮舉證否） | 採納為事實 A13 | SSOT |
| F14 | W7 ID 在文件與程式碼指兩條不同路線；W3／R1 未登記；V7 指向不存在的路線 | 修：改名 W5_markeronce、補登 W3／R1／W11／Z 家族、V 表重寫 | 本檔 A6／A12、design D5 |
| F15 | A9（鏡射不對稱）是 SSOT 宣稱但未實作的斷言 | 修：實作 `mirror_asym` 斷言（A9） | `plan013.py` |
| F16 | 模式 2 的左鏈組別對模式盲免疫，W1＝10/20 的守門機制未記錄 | 修：寫成事實 A14＋結構斷言 | 本檔、`plan013.py` |
| F17 | C4 累計硬砍在結果表格上可能呈現為全綠 | 記錄為 C4b＋以 B7 的牆鐘餘裕規避；引擎修正屬另案 | 本檔 C4b |
| F18 | `testcase_plan` literal 進入生產 bundle → 可離線算答案硬編碼 | **接受殘餘**：repo 本身公開，出貨測資輸入原已可見；apcs006／apcs009–012 同此，非本 change 新增。建議另開 change 處理（生產期遮蔽 literal 值＋私有測資），不以測資調整規避 | 本檔 C11 |
| F19 | A3 引用的 n=6000 極端輸入不存在於出貨資料且違反 A5／A8 | 修：降級為附註並標明「探針輸入、非出貨形狀」（A3） | 本檔 A3 |
| F20 | C5「逐字比對」過度描述（實為 trimEnd 容忍） | 修：改寫 C5 | 本檔 C5 |
| F21 | 小 n 範例筆（1／2／11／12）對窮舉形狀解毫無鑑別力 | 修：非範例筆最大 n 一律 ≥ 20，範例筆明記豁免（A15） | `plan013.py` |
| F22 | W4「左右岔顛倒」措辭有一種讀法是正確解 | 修：改寫為指向實作語義並註記對稱性屬收編（A6b） | 本檔 A6b |

### R1 稽核輪裁決（21 條確認，8 條 lens）

| ID | 發現 | 處置 | 落點 |
|----|------|------|------|
| R1-1 | 殺手帶 18 條大球數測試全落在週期整倍數上 → 「大球數就丟最大袋」20/20 | 修：逐條選非退化 I，並加**逐條**（非 per-entry any）退化斷言；Z4 路線入 CAPS（eq 15） | `plan014.py`、`semantics014.py::route_Z4_bigmax` |
| R1-2 | 大球數測試全為 D ≤ 4 → 寫死模數 8 的誤解 20/20 | 修：每筆殺手帶至少一條「大球數且週期 > 8」，並加固定模數族全掃斷言（B9c） | `plan014.py` |
| R1-3 | 零洞察鑑別斷言是 per-entry `any()`，承擔鑑別責任的行可整批退化 | 修：殺手帶改逐行斷言 | `plan014.py` |
| R1-4 | **K 球批次攤平**把 op 斷崖整條穿過（20/20） | 記錄為平台層殘餘 B3b／C13：op 可被 K 稀釋、C3 對同步碼無效、C4 只在整場超時中斷 → 測資無法封殺；建議另開引擎面 change | 本檔 C13／B3b、`routes/r014_batch32.py` |
| R1-5 | `sys.settrace(None)` 直接凍結 op 計數（全場 5 ops） | 記錄為平台層殘餘 C12（影響全站所有 op 斷崖題），不以測資規避 | 本檔 C12 |
| R1-6 | 每筆 5,000 ms 軟旗標實際上不會觸發（本 change 初版誤用為閘門） | 修：C3 改寫為「對同步碼無效」；移除步數下限（B5b）；成本閘刪除牆鐘模擬 | 本檔 C3、`plan014.py`、`cost_gate.mjs` |
| R1-7 | L1 收編路線的遞迴寫法在第 8 筆跳閘（19/20） | 修：預算改以「每筆 Σ2^(D−1) ≤ 600,000」表示（由遞迴寫法 13 ops/節點導出），並重配 D 值 | `plan014.py::COOPT_NODES_CAP` |
| R1-8 | P1 收編路線的守門量測錯了對象（per-group max 而非 per-entry 總和） | 修：改為每筆總和且套用到全部 20 筆 | `plan014.py::PERIOD_BALL_CAP` |
| R1-9 | 「只讀第一個標記＋位置規則」在舊佈局拿 20/20 | 修：混合筆一律 5 組、9 種模式序列兩兩相異；加家族上界斷言（A16，上界 12） | `plan013.py::POSITIONAL_RULE_CAP` |
| R1-10 | 第 3 筆被「形狀字典＋小 n 窮舉」攻破（3/20 > 範例地板） | 修：W12 入 CAPS（eq 2）並把第 3 筆最小組別拉大 | `semantics013.py::route_W12_dictbrute` |
| R1-11 | 斷言牆只看得到「輸出對不對」，看不到成本軸 | 修：新增 `cost_gate.mjs`，對出貨 literal 逐筆重跑並套用 C1／C4，把成本契約也機械化 | `curation/cost_gate.mjs` |
| R1-12 | A15 宣稱的機制（n ≥ 20 讓窮舉不可行）與實測不符 | 修：A15 改寫為真正的機制（W11／W12 的 eq 2 契約才是封殺來源） | 本檔 A15 |
| R1-13 | apcs013 題面暗示「模式 2 得另寫一套」，與收編路線 R1 抵觸 | 修：改寫該句 | `curation/page013.md` |
| R1-14 | apcs014 題面與 proposal 的全稱句實測為偽 | 修：兩處改為實測為真的敘述（限定於「每球至少一個 line 事件」的寫法） | `page014.md`、`proposal.md` |
| R1-15 | C6 的證據腳本未入庫、A4/A5 引用不存在的檔案 | 修：`probe.mjs`／`probe_depth.py` 入庫 `curation/` | `curation/` |
| R1-16 | P1 的大整數位元場寫法慢到佔 C4 預算 77% | 記錄為 B8b：語義正確、實測仍 20/20，但屬已知慢寫法，成本閘豁免時間警戒線 | `cost_gate.mjs`、本檔 B8b |

### R2 稽核輪裁決（18 條確認，6 條 lens）

| ID | 發現 | 處置 | 落點 |
|----|------|------|------|
| R2-1 | **（critical，R1 修法的直接副作用）** 殺手帶改用 D=3 後，逐條非退化斷言把可用殘值砍到只剩 2 個，答案塌縮成「I 的奇偶」→ 逐球模擬＋兩行硬規則 20/20 | 修：大球數測試的週期下限訂為 16（D ≥ 5），bulk 改 D=5；並把「小模數查表」收成路線檔實測（現 15/20） | `plan014.py::MIN_BIG_SPAN`、`routes/r014_smalloracle.py` |
| R2-2 | 固定模數族的豁免門檻取自**出貨資料**最大週期 → 寫死 M=512 拿 20/20 | 修：豁免門檻改取自**值域**最大週期；層數上限降為 17 使 P1 付得起最壞情形（兩者互斥的解法） | `plan014.py`、`page014.md`、spec |
| R2-3 | 步數上限程式（85M）與文件（55M）不一致；`KILL_STEP_MIN` 死常數仍在且註解仍主張 C3 有效 | 修：常數與文件一起改為 70M；刪除死常數 | `plan014.py`、SSOT／design／spec |
| R2-4 | **（R1 修法的副作用）** 「完全不讀模式標記、兩種讀法選較淺者」的啟發式 14/20，高於所有契約上界 | 修：收成 W13 路線（cap ≤ 12），第 12／13／15／20 筆各補一組「正確讀法反而較深」的動線 → 11/20 | `semantics013.py::route_W13_depthguess` |
| R2-5 | A16 只算「位置」一種鍵；同族改用「組別大小名次」可拿 18/20 | 修：家族上界改為三種鍵取最大；混合筆組別一律依大小遞減排列 | `plan013.py`（`_by_position`／`_by_sizerank`／`_by_count_only`）|
| R2-6 | 帶著週期洞察的「模擬整個週期建表」寫法只有 12/20，比毫無洞察的逐球模擬還低（階梯倒掛）| 修：P1 預算模型改用該寫法（成本 ∝ (D−1)×2^(D−1)、與球數無關）| `plan014.py::p1_ops` |
| R2-7 | 成本閘只比對輸出長度、檔頭仍宣稱套用 C3 軟旗標 | 修：改為逐字比對（probe 回傳完整輸出）、檔頭改寫 | `cost_gate.mjs`、`probe.mjs` |
| R2-8 | R1 的題面修正自己又寫進「超過單筆測資的時間上限」這道不存在的閘門；機台幾何在最底層自相矛盾；「接近 10^7」非實測值 | 修：三處改寫為實測為真的敘述 | `page014.md` |
| R2-9 | apcs013 資料在 R1 放大後，A0／spec 的 ops 與 bytes 全部沒重測（偏低 46–58%）| 修：全部重測回填（ref 58,276／gen 107,501／index 38,872／鏡射 120,475／最大筆 30,998 bytes）| SSOT A0／A8、spec |
| R2-10 | A4／A5 引用的探針腳本仍不在 repo | 修：`r013_scan_naive.py`／`r013_recursive_plain.py` 入庫 | `curation/routes/` |
| R2-11 | A13 對「模式標記為何無法自我推導」的理由寫錯 | 修：改為「兩種讀法通常都合法但答案不同」 | SSOT A13 |
| R2-12 | R1 commit 誤入 4 個 `__pycache__` 產物 | 修：untrack ＋補 `.gitignore` 規則 | `.gitignore` |
