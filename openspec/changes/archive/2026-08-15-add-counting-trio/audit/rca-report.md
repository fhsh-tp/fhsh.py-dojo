# `add-counting-trio` 兩輪未收斂 根因分析報告

> 分析者：乾淨脈絡代理（未參與本 change 的撰寫或修復）。所有數字均由我本機重跑／自寫腳本產生，下文標「實測」者皆附指令或檔案行號。

---

## 一句話結論

**這個 change 從一開始就不可能在兩輪內收斂**：它把「單一真相來源」做成一份 146 行、無任何程式讀得懂的 Markdown 表格，替自己開出約 **167 條同步義務（59 列事實 × 108 個位置欄格子）**，而唯一有執行器守門的表面（三份題目頁）在 R2 拿到 **0 findings**——沒有執行器的五個表面吃下 R2 全部 13 條。

---

## 收斂失敗的判定：**品質／流程問題為主（約 85%），標尺漂移為輔（約 15%）**

四份分析在這題上互相矛盾（角度 1、3 說「不是標尺」；角度 2、4 說「兩者皆有」，角度 4 甚至估到 46%）。我自己算過帳，裁決如下。

### 第一步：去重。帳面 8 → 13，實際是 6 → 10

我用 `factIds` 逐條比對 `audit/findings.json`，確認跨 lens 重複計數：

| 輪次 | 帳面存活 | 重複 | **相異缺陷** |
|---|---|---|---|
| R1 | 8 | `L1-1`／`W3-1`／`W5W6-1` 三條同指「jsonl 缺 4 筆紀錄」→ 併為 1 | **6** |
| R2 | 13 | `L1`≡`LB-5-W6`（W6）、`L2`≡`LB-2-W5`（W5）、`L3`≡`C-3`（S9）→ 各併 1 | **10** |

帳面「8→13」把增幅誇大了約 30%；但去重後仍是 **6→10，方向不變**。

### 第二步：分母也變了，但密度仍然上升

- R1：5 lens／**100** checks；R2：6 lens／**116** checks。
- 扣掉 R2 才可能存在的 Lens A（R1 修復回歸，17 checks），可比範圍為 100 vs 99 checks。
- **每百檢查的相異缺陷密度：R1 = 6.0 → R2 = 10.1。**

這是本次 RCA 最關鍵的一個數字。R1 確實修掉了 8 條具體實例，但**文件的缺陷密度不降反升**——這正是「修實例而非修類別」的統計指紋。角度 3 主張「總量等於凍結、所以不是品質問題」，只算了分母沒算密度，結論方向對但論證不完整。

### 第三步：逐條歸因 R2 的 10 個相異缺陷

| 成因 | 條數 | 具體 |
|---|---|---|
| **R1 的修復自己造出來的新缺陷** | 3 | W6 寫 2 ms（實測 4.0）、W5 引用不存在的 `factorial017_pre_recovery`、S9 位置欄宣稱 spec 有 tags（spec 全文無） |
| **R1 只修一半（改了矩陣列、沒同步位置欄）** | 2 | W1、G8 皆宣稱應落在 design.md，實測 design.md 完全沒有 |
| **R1 覆蓋不足（同 lens 射程內，但當輪沒抽到）** | 3 | E11、C3、G1——`E11` 與 `C3` **不在** R1 Lens 1 的 `verifiedClean` 名單裡（從未被檢查過）；`G1` 在 R1 被標 clean，但 R1 驗的是公式值、R2 驗的是出處句 |
| **標尺確實移動** | 1–2 | `E-F1`（SHALL 無斷言牆）與 R1 被駁回的 `L4-1` 是**同一類**卻判決相反；`F-015-polyfit` 屬 R1 Lens 5 名義射程內但未列舉的攻擊族 |

**標尺可解釋 1–2 條／10 條 ≈ 10–20%。** 角度 4 把 4 條位置欄 findings 也算成「新判準」而估到 46%，這站不住腳：R1 的 Lens 2 標題白紙黑字就是「三份文件與追溯矩陣的同步性」，同一判準只是抽了 3 列樣本；那是覆蓋率問題不是標尺問題。角度 3 估 0% 也過頭：`L4-1` 被駁、`E-F1` 存活，同類反轉是事實。

### 第四步：決定性證據——母體從未關閉，第三輪必然再漲

我自寫解析器掃 `trace-matrix.md`（59 列、108 個非「—」的位置欄格子）並反查兩份派生文件：

```
rows claiming spec: 46   claiming design: 47
spec.md 有 (trace X) 標記：40 個 distinct
design.md 有 (fact X) 標記：18 個 distinct
MISSING in spec (11): D3 C1 B1 B5 S1 S2 S3 S4 S6 S9 W2
MISSING in design (29): D1..D7 C1 C8 E4 E5 E9 E10 F3 F6 G3 G6 G7 G8 B6 S1..S5 S8 W1 W2 W4
反向違規（矩陣說不該在 spec，spec 卻有）：C2 C4 D6 W3 W5
```

其中 **6 列（C1／E4／E5／F3／F6／G3）的宣稱數字在 design.md 全文一個都找不到**（我用數字 token 硬比對，非標記慣例），另加人工確認的 W1、G8 共 8 條硬缺陷。**R2 的 Lens C 只抽驗 5 列就命中 3 條**，母體裡至少還躺著 5–20 條同型缺陷。

> **裁決**：不是「門檻在動所以看起來沒收斂」，是「缺陷產生速率不低於清除速率，且母體遠大於三輪能清的量」。就算把標尺凍死在 R1 深度，R2 仍會有 8–9 個相異缺陷，離歸零一樣遠。

---

## 去重後的根因清單（按解釋力排序）

### RC-1 ── 追溯矩陣是散文，全 repo 沒有任何程式讀它；`tasks.md 6.2` 承諾的執行器不存在，任務卻已打勾

**解釋了 26 條中的 14 條**：R1 `L1-1`／`L1-2`／`L1-3`／`L1-4`／`W3-1`／`W5W6-1`／`G8-1`；R2 `L1`／`L2`／`LB-1-E11`／`LB-2-W5`／`LB-3-G1`／`LB-4-C3`／`LB-5-W6`

**我的實測**：

| 檢查 | 結果 |
|---|---|
| `ls verify/check_number_traceability.py` | **不存在**；`tasks.md:32` 為 `[x]`，驗收條件明寫「新增 `verify/check_number_traceability.py` 並執行」 |
| `grep -rn trace-matrix --include=*.py --include=*.ts` | 5 處命中**全是註解散文**，零程式讀取 |
| `grep -rn "openspec/changes" package.json .github/` | 沒有任何 CI／script 會跑 `verify/*.py` |
| `python3 -c` 讀 `measure/routes015.json` | `process_start_floor_cpython_ms=14.84`、`max_entry_algorithm_increment=2.04`、`spelling_wall_pct={16.43, 4.29, 6.07}` |
| `trace-matrix.md:64` ＋ **已出貨的** `specs/counting-trio-challenges/spec.md:37` | 寫 `14.53／0.86／4.62／3.18／5.01`——**五個數字全錯**，且 16.43% vs 4.62% 差 3.6 倍 |
| `python3 measure/opprobe_015_nbound.py`（跑完） | 直接實測印 `4,011／1,008,011／1,510,511／2,509,512`；矩陣 C3 卻宣稱「三支獨立工具量到同一個 1,008,010」 |
| `grep -rl 216759740 .` | 只命中 `trace-matrix.md`／`spec.md`／`findings.json`——**三份散文**。`routes015.json` 的 `cellscan.per_entry_ops[8]` 是 `null` |

最後一項是 R1 修復方向錯誤的鐵證：`L1-2` 指控 E8 的 op 數量自未進 repo 的探針，R1「修好了」——**把一個沒出處的數字換成另一個沒出處的數字**，而它現在撐著 `spec.md:41` 的一條 SHALL。

**既有防線為何失效**：矩陣的「使用規約第 1 條」（每個數字要對回 fact id）是寫給人看的散文自律；斷言牆（`plan01*.py`）守的是**結論的性質**（`wall_clock_dominated_by_process_floor` 為真、佔比 ≤25%），不是文件裡的數字——`14.84/2.04/16.43` 下這些斷言全部通過，所以矩陣寫成 `14.53/0.86/4.62` 時斷言牆完全沉默，矩陣的證據欄卻宣稱「斷言 E-3 在此性質改變時會叫」，給了假保證。

**是否歷史教訓復發**：**是，且是被明文開過藥方後的復發。**
- `archive/2026-08-06-add-bracket-check-duo/rca-convergence-report.md:167` 已開出 **T-1 `scripts/trace-reconcile.ts`**（估 50–200 行，含錨點存在性／反向標記／數值白名單三項檢查），並預測「回溯可抓 R1 4 條、R2 4 條、R3 6 條」。
- `archive/2026-08-13-add-judge-deadline/rca-convergence-report.md:153` 已開出 **I-17「量測表格一律由腳本派生，禁止手抄」**（`measure/derive.py`，核心 <30 行）。
- 我實查：`scripts/trace-reconcile.ts` **不存在**、任何 `derive*.py` **不存在**。兩帖藥方都沒抓。

---

### RC-2 ── 「位置欄」憑空製造 108 條同步義務，卻用中文段名去指英文標題，任何工具都解析不到

**解釋了 4 條**：R2 `L3`／`C-1`／`C-2`／`C-3`（再加上我掃出的 8–29 條未報同型缺陷）

**我的實測**：
- `grep -n "13.3\|11.3\|625\|441\|375" design.md` → **零命中**（W1 的位置欄寫 design）
- `grep -n "bytes_of_bigint\|loglog\|314\|記憶體\|memory" design.md` → **零命中**（G8 同上）
- `grep -n "tags\|欄位集合" specs/.../spec.md` → **零命中**（S9 的位置欄寫 spec）
- 矩陣位置欄用的錨點是「量測表／驗收條件／決策表／成本階梯／殘餘風險」等中文標籤；`design.md` 的標題是 `Context`／`Decisions`／`Measurement methodology`／`Implementation Contract`／`Risks`——**字串交集為 0**。「量測表」被十餘個 design 格引用，而 design.md 根本沒有量測表這一節。
- `design.md` 只有 **18 個** `(fact X)` 標記，卻有 **47 列**宣稱要落在 design。（附帶更正：角度 1 說 36 個、角度 4 說 0 個，兩者都錯，實數是 18。）

**既有防線為何失效**：矩陣使用規約第 4 條（改一列要檢查位置欄每一處）由零個機制強制。更諷刺的是，R1 修復新增的 S9 列**自己就違反了它**——新增列的位置欄從未被任何人檢查過一次。

**是否歷史教訓復發**：**是，一字不差。** bracket-check-duo RCA:11 已寫明「追溯矩陣宣稱是 SSOT 但不可機械解析（design 錨點 9 個中 6 個指向不存在的分區、repo 內零工具讀它），同步殘留佔比 R1 14% → R2 33% → R3 **75%**」。本次規模更大（108 格）。

---

### RC-3 ── 修復輪沒有出口閘門，且與初版壓在同一個 commit；**R2 一半的相異缺陷是 R1 的修復自己造的**

**解釋了 5/10 個 R2 相異缺陷**：`L1`／`LB-5-W6`（W6）、`L2`／`LB-2-W5`（W5）、`L3`／`C-3`（S9）、`C-1`（W1）、`C-2`（G8）

**我的實測**：
- `python3` 逐行讀 `measure/browser-verification.jsonl`（14 筆）：index 10–13 即 R1 補進的四筆 `probe_import_math`／`probe_factorial_small`／`factorial015_rerun`／`ref017_after_unclean`。**補對了**。
- 但 `probe_import_math.max_ms = **4.0**`，而 R1 同一次動作在 `trace-matrix.md:136` 寫下「max **2 ms**」——**證據與引用它的文字同一次寫成卻互相矛盾**。
- `trace-matrix.md:135` 新增一句「其前一筆 `factorial017_pre_recovery` 為 0/20」；`grep -rn pre_recovery` **全 change 零命中**，`ref017_after_unclean`（index 13）的前一筆實際是 `factorial015_rerun`（index 12，還是 apcs015 的量測）。這是**為了回應 finding 而編造了一個不存在的證物**。
- `git log --oneline -- openspec/changes/add-counting-trio` → **只有 `e12f3cf` 一個 commit**。R1 的六項修復被 squash 進初版，**無法 diff、無法回歸、無法界定「本輪新增了什麼」**。commit message 自陳「稽核修正：補回四筆漏寫進 repo 的瀏覽器量測……」——修復的存在只能靠 message 自述。

**既有防線為何失效**：對抗覆核只覆核「稽核者的指控是否成立」，不覆核「修復者是否引入新缺陷」。修復完成後直接進下一輪，於是修復新增的表面在 R2 才第一次被看到，並被記成 R2 的**新** finding，直接推高計數。

**是否歷史教訓復發**：**是，教訓 4（修訂新增的規範內容須先自審）＋ 教訓 7（加保證要做副作用檢查）雙重復發。** W5 那句補述正是典型的「為消除不確定性而新增的保證」，它與 jsonl 的實際順序互斥。

---

### RC-4 ── 收斂判準用「存活條數」，但條數 = 抽樣強度 × 缺陷密度；未去重、未凍結掃描範圍

**解釋了**：R1 三條重複、R2 三對重複，以及「8 vs 13」這個比較本身的無效性

**我的實測**：`lensSummary` 其實**已經記了** `checksDerived`（R1 五個 lens 31/14/16/24/15＝100；R2 六個 lens 17/49/5/11/21/13＝116），資料就躺在檔案裡，但收斂判準完全沒用到它。同步性 lens 從 14 checks 縮到 5、證據 lens 從 31 漲到 49——兩輪合法地各抽了一小把不重疊的樣本。

**是否歷史教訓復發**：是，教訓 6 的算術面。但本案暴露教訓 6 的**處方寫得不夠**：只凍結「深度」不夠，還必須凍結「掃描範圍」，並把計數單位定義為「相異缺陷」。

---

### RC-5 ── 對抗覆核只輸出布林，不記裁決理由；同類反覆、重提重駁，且至少駁錯一次

**解釋了 6 條**：R1 `L1-4`／`L4-1`／`L5-1`；R2 `E-F1`／`E-F2`／`F-017-avg-family`

**我的實測**：
- `findings.json` 的欄位是 `id／severity／factIds／where／claim／evidence／consequence／suggestedFix／fixRisk／lens／survivedAdversarialReview`——**沒有任何欄位記錄駁回理由**。裁決無法被重用，也無法檢驗一致性。
- 同類反轉：`L4-1`（spec 的 SHALL NOT 無自動掃描）R1 被駁；`E-F1`（spec 的 SHALL 無斷言牆）R2 存活。
- 重提重駁：R1 `L5-1` 與 R2 `F-017-avg-family` 是逐字同一條（apcs017 平均族 15/20 > 文件引用的 12/20），被提兩次、駁兩次。
- **駁錯一次的實證**：`L1-4` 被駁，但我實跑 `judge_ops.count_ops_source('import math\nprint(1)\n','')` 連兩次得 **7／7**，而 `design.md:64` 寫「`import math` costs 278 events cold and **zero warm**」、矩陣 C4 寫「行內第二次 **21**」——**兩個數字都不對**，這條該活的被殺掉了。

**最關鍵的副作用**：把「缺機械保證」型 findings 系統性駁回（`L4-1`），等於讓 **RC-1 這個真正的根因永遠進不了修復清單**——兩輪都只在打它產生的實例。

**是否歷史教訓復發**：教訓 6 的子問題，本次新暴露（缺裁決 ledger）。

---

### RC-6 ── 量測器身分未統一、斷言牆覆蓋不齊

**解釋了 3 條**：R2 `LB-4-C3`／`E-F1`／`E-F2`

**我的實測**：
```
curation/plan015.py:  31 條斷言/problems
curation/plan016.py:  20
curation/plan017.py:  21
measure/measure016.py: 4
measure/routes017_measure.py: 0      ← 全檔 345 行，零斷言
```
`spec.md:84` 的「20/20 at 155, 156 and 166 operations」是一條 SHALL，其唯一依據就是這支零斷言的腳本——**改壞了不會有人叫**。
另外 `verify/judge_ops.py` 匯出三個外殼（`count_ops`／`count_ops_source`／`count_ops_source_fresh`），對同一條路線給出 `1,008,011`（opprobe 直測）與 `1,008,010`（routes015.json）兩個數，且沒有任何交叉一致性斷言；矩陣 C3 卻反過來宣稱「三支獨立工具量到同一個值」。**統一了「一個 op 是什麼」，沒統一「量測外殼算不算數」。**

---

### 附帶：一條沒被任何人報出來的真缺陷（我在本次 RCA 發現）

`spec.md:41` 的 SHALL 引用 `216,759,740` op；我 `grep -rl` 全 change，這個數字**只存在於三份散文**，`routes015.json` 的 `cellscan.per_entry_ops[8]` 是 `null`。也就是說 R1 對 `L1-2` 的修復，把一個查無出處的數字換成了另一個查無出處的數字，而它現在是出貨規範層的一條 SHALL。R2 沒抓到——因為 R2 的 Lens B 也是抽樣。

同時我獨立復現了 `F-015-polyfit`：用題目頁公開的 k=1..5 五點（0, 6, 28, 96, 252）做 Fraction 拉格朗日插值，對 k=1..1000 與正解逐項比對，**mismatch = 0**。這是真實的鑑別力破口，探針只需 15 行。

---

## 建議的防線

### A. 機械化，且應在**本 change 內**做（總計約半天到一天）

| # | 動作 | 攔下哪些 | 成本 |
|---|---|---|---|
| **M1** | 補上 `tasks.md 6.2` 承諾但不存在的檢查器。建議寫成 **`scripts/trace-lint.py`**（吃 change 目錄為參數，跨 change 重用），含三段：**(a) 引用解析**——矩陣證據欄的檔名／JSON 鍵／jsonl label 必須存在於**該列所引的那個檔**；**(b) 數字綁鍵**——矩陣與 spec／design 的每個數字必須等於所引 json 某鍵之值；**(c) 位置欄雙向**——位置欄非「—」的列，目標文件必須有對應標記，反向也查 | 26 條中的 18 條（含 R2 存活 13 條中的 10 條），以及我掃出的 8–29 條未報同型 | (a) 約 20 行、(c) 約 25 行、(b) 約 80 行 |
| **M2** | **先把矩陣證據欄改成可解析位址**（`measure/routes015.json#process_start_floor_cpython_ms`），否則 (b) 只能做「數字有出現在該檔任一處」的鬆檢查，會**漏掉 E11**（14.53 恰好命中檔內無關的 per-entry 牆鐘值） | 決定 M1(b) 能否真正零誤報 | 一次性改寫 146 行矩陣，1–2 小時 |
| **M3** | 統一 fact 標記慣例並補齊：`design.md` 現有 18 個 `(fact X)`、`spec.md` 44 個 `(trace X)`，先統一為一種，再補上約 29 處缺標記 | M1(c) 零誤報的前提 | 30 分鐘 |
| **M4** | **立即修正已出貨的錯數字**：`spec.md:37` 的 `14.53／0.86／4.62／3.18／5.01` → `14.84／2.04／16.43／4.29／6.07`（注意三種寫法的**順序**也錯了，最緊的是 rowscan 而非文件宣稱者）；矩陣 `W6` 2 ms → 4.0 ms；刪掉 `W5` 虛構的 `factorial017_pre_recovery`；`G1` 的「n ≤ 400」→「n = 1..299」（`semantics017.py:17,99,101` 白紙黑字，400 只是 `BRUTE_MAX_N` 效能守門） | R2 存活的 4 條事實錯 | 半小時 |
| **M5** | 把 `216,759,740` 這個只活在散文的 op 數收編成腳本輸出並持久化進 json（`count_ops_source_fresh` 對 `r015_cellscan.py` 在 n=249，單次約 19 秒，可設 `--slow` 旗標） | 未報的規範層裸奔數字 | 20 行 |
| **M6** | 替 `measure/routes017_measure.py` 補斷言牆（照抄 `measure016.py` 既有樣式），並加一支 10 行 meta-lint：凡寫出 `measure/*.json` 的腳本，輸出必須含 `problems` 鍵、非空即 exit≠0 | `E-F1`，以及未來所有「exit 0 所以以為受保護」的靜默失真 | 25 分鐘 |
| **M7** | `verify/judge_ops.py` 只保留一個公開量測入口；若三個外殼必須並存，在 `self_test` 內斷言三者對同一支參考腳本的差值等於已知常數。刪掉矩陣 C3「三支獨立工具量到同一個 1,008,010」這種無法機械驗證的寫法 | `LB-4-C3`，以及所有「同一路線在不同文件出現差 1 的 op 數」 | 1–2 小時 |
| **M8** | 禁止易變量進規範層：doc lint，`spec.md` 內出現 `\d+\.\d{2}\s*(ms\|%)` 即 fail。牆鐘只准以布林性質或整數區間出現，實數留在 `measure/*.json` | E11 型（兩位小數的牆鐘寫進 SHALL 句） | 5 行 regex |

### B. 機械化，屬**專案層級另案**

| # | 動作 | 理由 |
|---|---|---|
| **P1** | 把 `scripts/trace-lint.py` 掛進 `pnpm lint` 或 CI。**目前 `package.json` 與 `.github/workflows` 沒有任何一條路徑會執行 `openspec/changes/**/verify/*.py`**——`check_frontmatter_pair.py` 現在也是「有人記得跑」的狀態 | 腳本存在但沒人跑 = 不存在 |
| **P2** | `findings.json` schema 加必填 `rebuttalRationale`（駁回時）與 `adjudicatedIn`（沿用前輪裁決時）；下一輪開始前先讀已裁決 ledger，重提同一 claim 必須引用新證據 | `L5-1`／`F-017-avg-family` 重提重駁；`L4-1` vs `E-F1` 判決反轉 |
| **P3** | 收斂判準改為「**每百檢查的相異缺陷數**」，並在 round 0 先凍結 **lens 清單＋每 lens 的 checksDerived 目標＋掃描範圍**。對抗覆核階段先做等價類合併（同 factId ＋同 where ＋同 suggestedFix 視為同一條）再輸出存活數 | 「8 vs 13」這種分母不同、未去重的比較根本量不到收斂 |
| **P4** | 跨題可重用的攻擊族基線掃描器：每道計數題自動跑三組通用突變——(1) 用題面公開的範例點做低次多項式／差分插值、(2) 正解合併運算子替換（min→avg／sum//k）、(3) 常數 ±1 與迴圈邊界 off-by-one；得分高於題目宣稱的誤解上限即 fail | `F-015-polyfit` 我實測 20/20，探針只需 15 行 |
| **P5** | **修訂全域守則第 6 條**：凍結標尺不只凍結「深度」，還要凍結「掃描範圍」；每輪由矩陣機械派生完整檢查清單，逐項標 pass／fail／skip，skip 必須寫理由，下一輪從 skip 清單開跑 | 本案兩輪各自合法抽了不重疊的樣本，存活數變成抽樣位置的函數 |

### C. 靠人記得（無法機械化，應誠實標明「未受保護」）

| # | 項目 | 為什麼不可機械化 |
|---|---|---|
| **H1** | 修復輪**必須獨立 commit**，且 commit 前跑一次 `trace-lint.py --diff`（只檢查本輪新增的矩陣列與 jsonl 紀錄）。修法方向規定為「重跑腳本 → 用輸出覆寫散文」，**禁止「補資料去符合既有散文」** | 流程紀律。但 `--diff` 模式本身可機械化（約 20 行） |
| **H2** | 語意誤述（R1 `L3-1`「最高可達到的階級」vs「成功兌換的次數」）：兩種措辭字串合法、數字不出現，deterministic 檢查抓不到。可**部分**降級——「輸出是什麼」那句話由 `curation/semantics0XX.py` docstring 單一來源產出，spec 與 frontmatter description 逐字引用，改成字串相等比對 | 語意等價判斷 |
| **H3** | 矩陣 D 段（維護者訪談決策）本質無外部真值，只能檢查「有沒有被引用」，不能檢查「對不對」 | 同上 |
| **H4** | 攻擊族列舉的**完備性**：可以機械化「已列舉的攻擊族有沒有跑分」，無法機械化「列舉是否窮盡」。`F-015-polyfit` 要先有人想到「答案是四次多項式」 | 這條線不能承諾自動化到零 |
| **H5** | 明文裁決 `F-017-avg-family`（兩輪皆被提、兩輪皆被駁）：需要一條寫下來的裁決而非重複駁回 | 需維護者權衡 |

**建議實施優先序**：M4（先止血，錯數字已上線）→ M2＋M3（鋪路）→ M1（主力）→ M6＋M7 → H1 → P1 → P3。理由：M1 一支腳本回溯攔下 18/26 條，且 M4 是唯一有**外部可見損害**的項目（錯數字已寫進 `specs/counting-trio-challenges/spec.md`）。

---

## 誠實的執行限制

**我沒有查到／無法查的**

1. **瀏覽器實測完全沒有復現。** 全部 W 段事實（三題 20/20、`cellscan` 8/20、`factorial` 0/20、`probe_factorial_small` 19/20）都建立在 `measure/browser-verification.jsonl` 這一份人工貼上的檔案。我只驗證了「矩陣文字 vs jsonl 內容」是否一致，**沒有驗證 jsonl 本身是否為真**。這是本 change 最大的未受保護面，也是四份分析共同的盲區。
2. **「R1 之前 jsonl 只有 10 行」無法從 git 驗證。** 修復被 squash 進 `e12f3cf`，我只能採信 R1 `L1-1` 的敘述加上現況 14 行反推。RC-3 中「R1 補了四筆」這一點是**單一來源**（finding 敘述 ＋ commit message 自述）。
3. **`plan01*.py --check` 三支斷言牆我沒有完整重跑**（只做了 `grep` 計數與讀關鍵段落）。「斷言牆全數通過」沿用 R1 Lens 4 的宣稱。
4. **牆鐘可復現性我沒有實測。** 角度 2 宣稱以 min-of-7 重跑得 14.26／14.95／14.08 ms（與登記值 14.84 差約 6%），我**沒有復現這一項**——若屬實，代表「兩位小數的牆鐘常數」原理上就是無限 finding 產生器（這也是 M8 的理由），但目前它是單一來源。
5. **位置欄缺漏的母體大小有不確定區間。** 嚴格按標記慣例是 design 缺 29／spec 缺 11；用「claim 的數字是否出現在目標檔」硬比對只確認 6 條（C1／E4／E5／F3／F6／G3）＋ 人工確認 2 條（W1／G8）。**真實母體介於 8 與 29 之間**，我傾向偏高端（因為 D 段等無數字的列無法用數字法判定）。四份分析給出 19／19／29／29 四個數，都沒有標示這個區間。
6. **我沒有驗證對抗覆核是否由獨立代理執行**，也無從得知 `survivedAdversarialReview: false` 的三／二條各自的駁回依據——`findings.json` 根本沒有這個欄位（這正是 RC-5）。

**我更正的、四份分析中的事實錯誤**

| 主張 | 出處 | 我的實測 |
|---|---|---|
| design.md 有 36 個 `(fact X)` 標記 | 角度 1 | **18 個** |
| design.md 有 0 個 fact 標記 | 角度 4 | **18 個** |
| spec.md 有 40 個 `(trace X)` | 角度 1／4 | **44 個**（distinct id 40 個） |
| R2 位置欄缺漏「design 19 條」 | 角度 1／3 | 嚴格計為 **29 條**，硬確認 8 條 |
| 「標尺沒漂，比例 0%」 | 角度 3 | `L4-1` 被駁 vs `E-F1` 存活是同類反轉，**約 10–20%** |
| 「標尺解釋 46%」 | 角度 4 | 把 4 條位置欄 findings 誤算為新判準；R1 Lens 2 就是同步性 lens。**高估** |

**一句話給維護者**：這個 change 的三道題目本身（題面數字、literal、判題契約、20/20 驗收）我抽驗都經得起重跑；壞掉的是「證物 → 規範文字」這條產線。一支 30 秒跑完的 130 行腳本能做完的事，被排進了兩輪各數小時的人工稽核，而那支腳本在兩份前置 RCA 裡已經被開過兩次藥方（`T-1`、`I-17`），都沒抓。