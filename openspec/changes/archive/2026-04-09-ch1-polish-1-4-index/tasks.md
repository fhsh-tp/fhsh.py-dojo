## 1. 1-4.md 知識地圖一致性校驗

- [x] 1.1 逐項核對 ASCII 知識地圖 (L30-49) 的 1-1 區塊：確認是否需要新增「求值模型（由內而外）」或「引號規則」等 1-1 在 TBD 填補後新增的教學點。若知識地圖的項目與 1-1 修改後的教學重點不符，更新項目文字
- [x] 1.2 逐項核對知識地圖的 1-2 區塊：確認是否需要反映 `int(input())` 求值模型的顯性化（M-1 mental model rule callback）。評估是否需要新增一個子項
- [x] 1.3 逐項核對知識地圖的 1-3 區塊：確認項目清單是否完整反映 1-3 修改後的教學內容

## 2. 1-4.md 自我檢查表一致性校驗

- [x] 2.1 逐項核對 12 個自我檢查項目 (L56-72) 與 1-1 ~ 1-3 修改後的教學目標。重點關注：1-1 新增的「引號不能混用」和「由內而外求值」概念是否應反映在檢查表中
- [x] 2.2 若有教學點在修改過程中被強化但未出現在檢查表，新增對應的檢查項目。保持檢查表的簡潔（每項一行），不超過 15 項

## 3. 1-4.md P-1 punctuation style rule 標點風格

- [x] 3.1 掃描 `docs/tutor/py/ch1/1-4.md` 中所有 `——` 的使用。按 P-1 規則修正例行子句的 `——` 為逗號或冒號

## 4. 1-4.md S-2 post-humor connector rule 語氣銜接

- [x] 4.1 L18 的 `(ﾉ◕ヮ◕)ﾉ*:･ﾟ✧` 後 L20 缺 callback connector：在 L20 開頭加入回歸連接詞（例如「真的值得灑花。不過好吧...」），將慶祝語氣連回敘事
- [x] 4.2 L20 的 `_(´ཀ`」 ∠)_` 後接 `---` 再接 H2（L24），跨 H2 邊界無連接詞：在 `---` 前加一句回歸句（例如「不過別擔心，你現在的基礎已經比當時的我好多了。」）
- [x] 4.3 L75 的 `σ(´∀｀*)` 後接 `---` 再接 H2（L79），跨 H2 邊界無連接詞：在 `---` 前加一句回歸句（例如「所以不要灰心，你已經在路上了。」）

## 4b. 1-4.md S-3 section transition rule 段落過場

- [x] 4b.1 `## 模組一知識地圖`（L24）前補 2-4 句 H2 過場：總結前面學了什麼 → 說明為什麼要回顧 → 引出知識地圖
- [x] 4b.2 `## 自我檢查表`（L54）前補 2-4 句 H2 過場：從知識地圖過場到自我檢測
- [x] 4b.3 `## 模組二預告`（L79）前補 2-4 句 H2 過場：從自我檢查過場到下一模組預告

## 4c. 1-4.md F-1 圖片佔位符雙行格式

- [x] [P] 4c.1 補齊圖 13（L28）的 `![📷 **圖 13**：...](path)` image link 行，放在 `> 📷` caption 行之前
- [x] [P] 4c.2 補齊圖 14（L95）的 `![📷 **圖 14**：...](path)` image link 行

## 4d. 1-4.md K-1 顏文字語氣密度

- [x] 4d.1 L13-20 有 2 個 kaomoji（L18、L20）在約 8 行散文中，超過 K-1 上限（1/10 行）。拉開間距：在 L18 和 L20 之間插入更多散文行，或將 L18 的 kaomoji 移到更前面的段落

## 5. index.md 校驗

- [x] 5.1 核對 `docs/tutor/py/ch1/index.md` 中 4 個章節連結 (L14-17) 的顯示文字是否與各節 frontmatter `title` 一致。若不一致，更新 index 中的文字（不改 frontmatter）
- [x] 5.2 核對 L10 的核心觀念描述（`程式語言只是工具，重點在理解...`）是否仍準確反映修改後的教學重心。若 1-1 的 TBD 填補引入了新的教學主軸（如求值模型），評估核心觀念是否需要微調

## 6. 最終校讀

- [x] 6.1 最終校讀 1-4.md 和 index.md，確認 prose 語氣與 1-1~1-3 一致，無遺漏
