## 0. Spec 修改

- [x] 0.1 將 K-1 多樣性子條款合併至 `openspec/specs/python-ch1-content/spec.md`：取代原有 K-1 requirement block，加入 3 條多樣性限制和 3 個新 scenario
- [x] 0.2 更新 `phoenix-popular-science-article-style-enhance.md` 的 K-1 段落，反映新增的多樣性子條款

## 1. 1-1.md EAL 修正

### 1A. P-1 標點風格

- [x] [P] 1.1 逐行掃描 `1-1.md` 所有 `——` 的使用，按 P-1 判定清單修正。已知違規：L19（因果→冒號）、L23（定義→冒號）、L112（定義→冒號）、L134（定義→冒號）、L319（因果→冒號）。保留 L21 `你猜怎麼著——學會之後`（戲劇性揭曉）

### 1B. T-1 術語前向引用

- [x] 1.2 修正 L72 `演算法（Algorithm）`：此術語未經解釋直接出現。移除正式術語或改用白話描述（例如「用考試的方式檢驗你用程式解題的能力」）

### 1C. S-1 類比 meta-cognitive bridge

- [x] 1.3 L23 的黑盒子比喻前補 bridge（例如「如果要用一個比喻來形容這種感覺：」）
- [x] 1.4 L44 的日本餐廳比喻前補 bridge（例如「為了理解程式語言扮演的角色，我們先想一個場景：」）
- [x] 1.5 L130 的 `print()` = 嘴巴/LINE 按鈕比喻前補 bridge（例如「如果覺得太抽象，用一個生活中的比喻來想：」）

### 1D. S-2 笑話回歸連接詞

- [x] 1.6 L40 `(ﾉ◕ヮ◕)ﾉ*:･ﾟ✧` 後接 H2（L42），非 H3 豁免。在 H2 開頭加回歸句（例如「帶著這些目標，我們先從最根本的問題開始：」）
- [x] 1.7 L50 `ʅ（´◔౪◔）ʃ` 後 L52 無回歸。加連接詞（例如「雖然很嚴格，但別擔心。」）
- [x] 1.8 L254 `σ(´∀｀*)` 後 L259 無回歸。加連接詞（例如「先別擔心那場爆炸——好了，...」）
- [x] 1.9 L326 `(๑•̀ㅂ•́)و✧` 後 L328 無回歸。加連接詞（例如「趁這股氣勢，下面...」）

### 1E. S-3 段落過場

- [x] 1.10 H2 `程式語言是什麼？`（L42）前補 2-4 句過場（從學習目標銜接到第一個主題）
- [x] 1.11 H2 `認識我們的 Judge 系統`（L70）前補 2-4 句過場
- [x] 1.12 H2 `自己動手試試！`（L324）前補 2-4 句過場
- [x] 1.13 H2 `本節小結`（L348）前補 1-2 句過場
- [x] 1.14 H3 `怎麼使用 Judge？`（L96）前補 1 句過場
- [x] 1.15 H3 `第二步：印出計算結果`（L165）前補 1 句過場
- [x] 1.16 H3 `第三步：印出多個東西`（L189）前補 1 句過場
- [x] 1.17 H3 `第二步：把讀到的東西印出來`（L234）前補 1 句過場
- [x] 1.18 H3 `Step 1：分析 IPO`（L284）前補 1 句過場
- [x] 1.19 H3 `Step 2：寫出每一行程式碼`（L292）前補 1 句過場
- [x] 1.20 H3 `Step 3：在 Judge 上測試`（L307）前補 1 句過場
- [x] 1.21 H3 `常見錯誤排查`（L311）前補 1 句過場
- [x] 1.22 H3 `類題二：鸚鵡學舌`（L338）前補 1 句過場

### 1F. C-1 程式碼前導

- [x] 1.23 H3 `第一步：讀取一行輸入`（L224）後的 code block 前補對話式前導
- [x] 1.24 H3 `第二步：把讀到的東西印出來`（L234）後的 code block 前補前導
- [x] 1.25 H3 `Step 2：寫出每一行程式碼`（L292）後的 code block 前補前導

### 1G. E-1 錯誤預防

- [x] 1.26 `print(1+1)`（L171）引入表達式列印處，補 Python 3 必須加括號的警告
- [x] 1.27 多引數 `print()`（L193）引入處，補逗號自動加空格的警告

### 1H. K-1 顏文字密度

- [x] 1.28 L17-27 開場段有 3 個 kaomoji 在約 6 行散文中（密度 1/2 行），需移除或重新分配 1-2 個

## 2. 1-2.md EAL 修正

### 2A. P-1 標點風格

- [x] [P] 2.1 逐行掃描 `1-2.md` 所有 `——` 和單 `—` 使用。已知違規：L38、L109（H2 標題用 `—` 作分隔）、L117（反問→逗號）、L157（定義→冒號）、L338-341（註解用 `—`）、L372（因果→冒號）。保留 L406（戲劇性預告，borderline）

### 2B. T-1 術語前向引用

- [x] 2.2 L161 `**型別轉換（Type Conversion）**` 在概念教學前出現。將正式術語移至 L170（`int()` 首次示範處），或在 L161 前加一句教學銜接

### 2C. S-1 類比 meta-cognitive bridge

- [x] 2.3 L48 置物櫃比喻（`### 生活比喻：置物櫃`）前補 bridge（例如「聽起來有點抽象？讓我們用一個你每天都會碰到的東西來比喻。」）

### 2D. S-2 笑話回歸連接詞

- [x] 2.4 L159 `ΩДΩ` 後 L161 無回歸。加連接詞（例如「別急，解法其實很簡單：」）
- [x] 2.5 L203 `(๑•̀ㅂ•́)و✧` 後 L205 WARNING 無回歸。在 WARNING 前加一句（例如「不過先別高興太早，有個小地雷要注意：」）

### 2E. S-3 段落過場

- [x] 2.6 H2 `自己動手試試！`（L366）前補 2-4 句過場
- [x] 2.7 H2 `本節小結`（L396）前補 1-2 句過場
- [x] 2.8 H3 `為什麼需要變數？`（L40）：H2 下第一個 H3，補 1-2 句
- [x] 2.9 H3 `為什麼要分型別？`（L111）：H2 下第一個 H3，補 1-2 句
- [x] 2.10 H3 `input() 的型別陷阱`（L141）前補 1 句過場
- [x] 2.11 H3 `型別轉換`（L166）前補 1-2 句過場（取代 TBD）
- [x] 2.12 H3 `基本運算`（L216）：H2 下第一個 H3，補 1-2 句（取代 TBD）
- [x] 2.13 H3 `除法的小驚喜`（L228）前補 1 句過場（取代 TBD）
- [x] 2.14 H3 `整數除法 // 與取餘數 %`（L242）前補 1 句過場（取代 TBD）
- [x] 2.15 H3 `運算優先順序`（L274）前補 1 句過場
- [x] 2.16 H3 `題目說明`（L294）：H2 下第一個 H3，補 1-2 句
- [x] 2.17 H3 `Step 1：分析 IPO`（L314）前補 1 句過場
- [x] 2.18 H3 `Step 3：逐行解讀`（L336）前補 1 句過場
- [x] 2.19 H3 `類題二`（L378）前補 1 句過場
- [x] 2.20 H3 `類題三`（L386）前補 1 句過場

### 2F. C-1 程式碼前導

- [x] 2.21 L345-347 的錯誤示範 code block（`❌ **忘記 int() 轉換**`）前補對話式前導

### 2G. E-1 錯誤預防

- [x] 2.22 `//` 和 `%`（L248）引入處，補常見錯誤警告（例如混淆 `//` 和 `/`、負數行為）

### 2H. T-3 空 UI 元素

- [x] 2.23 L99-100 `> [!TIP] 什麼是保留字？` 的 body 為空。填入內容或以 `<!-- DEFERRED: ... -->` 包裹

### 2I. K-1 顏文字密度

- [x] 2.24 整體密度約 1/8.7 行（超過上限 1/10 行），需移除 1-2 個 kaomoji

### 2J. T-2 殘留 TBD 標記

- [x] 2.25 解決並移除 L98 的 `<!-- TBD 這裡增加保留字的說明 -->`：填寫保留字說明或移除整段
- [x] 2.26 解決並移除 L123 `<!-- TBD 增加過場 -->`（與 2.10 合併處理）
- [x] 2.27 解決並移除 L125 `<!-- TBD 增加對型別的更完整說明 -->`：補充型別說明或移除
- [x] 2.28 解決並移除 L168 `<!-- TBD 增加過場 -->`（與 2.11 合併處理）
- [x] 2.29 解決並移除 L214、L218 `<!-- TBD 這邊增加過場 -->`（與 2.12 合併處理）
- [x] 2.30 解決並移除 L230 `<!-- TBD 這邊增加過場 -->`（與 2.13 合併處理）
- [x] 2.31 解決並移除 L238 `<!-- TBD 增加顏文字表情 -->`：加入合適的顏文字
- [x] 2.32 解決並移除 L244 `<!-- TBD 這邊增加過場 -->`（與 2.14 合併處理）
- [x] 2.33 解決並移除 L283 `<!-- TBD 增加顏文字表情 -->`：加入合適的顏文字

## 3. 1-3.md 殘餘修正

### 3A. P-1 標點風格

- [x] [P] 3.1 L408 `直接印 Leap Year——但 1900` 改為 `，但`
- [x] [P] 3.2 L449 `是指派**——搞混就完蛋` 改為 `：搞混就完蛋` 或 `，搞混就完蛋`

### 3B. S-1 類比 bridge

- [x] 3.3 L139 超商打折比喻的 bridge 強化：將「用一個你一定經歷過的場景來感受」改為更明確的 bridge（例如「為了讓你理解這件事，讓我們用一個你每天都會碰到的情境來打比方：」）

### 3C. S-2 笑話回歸連接詞

- [x] 3.4 L412 `Σ(ﾟДﾟ；≡；ﾟдﾟ)` 後 L414 核心觀念區塊無回歸。加連接詞（例如「這些血淚教訓告訴我們一件事：」）

### 3D. S-3 段落過場

- [x] 3.5 H2 `布林值`（L43）前補 2-4 句過場（從學習目標銜接到第一個主題）
- [x] 3.6 H3 `為什麼需要流程控制？`（L133）：H2 下第一個 H3，補 1-2 句
- [x] 3.7 H3 `if：最基本的判斷`（L148）前補 1 句過場
- [x] 3.8 H3 `為什麼要畫流程圖？`（L244）：H2 下第一個 H3，補 1-2 句
- [x] 3.9 H3 `實戰：閏年判斷的流程圖`（L257）前補 1 句過場
- [x] 3.10 H3 `題目說明`（L311）：H2 下第一個 H3，補 1-2 句
- [x] 3.11 H2 `自己動手試試！`（L420）前補 2-4 句過場
- [x] 3.12 H2 `本節小結`（L442）前補 1-2 句過場

## 4. 1-4.md 殘餘修正

### 4A. P-1 標點風格

- [x] [P] 4.1 L60 `不是考試那種——是給你自己確認用的` 改為 `：是` 或 `，是`

### 4B. S-2 笑話回歸連接詞

- [x] 4.2 L14 `(ﾉ◕ヮ◕)ﾉ*:･ﾟ✧` 後 L16 `回想一下` 無回歸。加連接詞（例如「說真的，回想一下...」）

### 4C. K-1 顏文字密度

- [x] 4.3 整檔約 23 行散文有 4 個 kaomoji（密度 1/5.75 行），需移除或重分配 1-2 個

## 5. 全檔 kaomoji 多樣性替換

- [x] 5.1 盤點全 ch1 的 kaomoji 使用，列出需替換的項目（跨章超過 3 次的 kaomoji）
- [x] 5.2 從 `phoenix-popular-science-article-style` kaomoji catalog 中選擇替換用的顏文字，確保情緒類別匹配且未在同檔/同章過度使用
- [x] 5.3 執行替換，確保每檔至少 2 個情緒類別、單檔不超過 2 次、跨章不超過 3 次
- [x] 5.4 最終全檔校讀，確認所有規則合規、prose 語氣一致
