## 1. P-1 punctuation style rule 標點風格統一

- [x] 1.1 逐行掃描 `docs/tutor/py/ch1/1-3.md` 中所有 `——` 的使用。對例行子句銜接改為逗號或冒號。保留以下正當用法：天氣場景 hook 的戲劇性敘述、閏年規則的「順序很重要」強調。記錄每處修改的行號與改法

## 2. C-1 conversational lead-in rule — Code Block 過場檢查

- [x] 2.1 檢查「比較運算子」(L60) 的 `print(3 > 2)` 等示範 code block 前是否有過場
- [x] 2.2 檢查「邏輯運算子」(L90) 的 `score >= 60 and attendance >= 80` code block 前是否有過場
- [x] 2.3 檢查「高中生活舉例」(L109) 的社團報名/段考免補考 code block 前是否有過場
- [x] 2.4 檢查「if 基本判斷」(L144) 的 `if score >= 60` code block 前是否有過場
- [x] 2.5 檢查「縮排說明」(L159) 的縮排正確/錯誤 code block 前是否有過場
- [x] 2.6 檢查「if-else 二選一」(L170) 的 code block 前是否有過場
- [x] 2.7 檢查「if-elif-else 多選一」(L185) 的成績等第 code block 前是否有過場
- [x] 2.8 檢查「巢狀 if」(L210) 的超商打折 code block 前是否有過場
- [x] 2.9 檢查「閏年布林表達式」(L320) 和「多層 if-elif-else 寫法」(L345) 的 code block 前是否有過場
- [x] 2.10 檢查「順序錯誤反例」(L376) 的 code block 前是否有過場

## 3. M-1 mental model rule — 求值模型顯性化

- [x] 3.1 確認 `(year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)` (L321-322) 的解說是否包含 step-by-step evaluation trace。具體需要追蹤的步驟：(1) 先算 `year % 4` 等 `%` 運算 → (2) 再算 `==`/`!=` 比較 → (3) 再算 `and` → (4) 最後算 `or`。若目前的分解說明 (L326-330) 已足夠清楚，確認其格式符合 trace 風格；若不夠，補上編號步驟
- [x] 3.2 加上對 1-1 `print(1+1)` 和 1-2 `int(input())` 求值模型的 callback reference（一句即可，例：「還記得 `print(1+1)` 由內而外的規則嗎？複雜的布林運算也一樣——先算最裡面的，再一層一層往外」）

## 4. S-1 analogy bridge rule — 類比 meta-cognitive bridge

- [x] 4.1 檢查「天氣決策」hook (L15-18)：作為開篇 hook，可能天然就是 bridge 角色——若已充分則跳過
- [x] 4.2 檢查「超商打折」比喻 (L135-139)：`你去超商買東西` 前是否有一句說明「為什麼要用買東西的例子來解釋流程控制」
- [x] 4.3 檢查「岔路口」比喻 (L133)：`到目前為止，我們的程式都是「直線型」` 是否構成足夠的 bridge

## 5. S-3 section transition rule — 段落過場密度

- [x] 5.1 檢查「布林值→if-elif-else」過場 (L127)：`好，你學會問是非題了。接下來要學的是——根據答案做不同的事。` — 評估是否達到 2~4 句標準。注意此處 `——` 也需要按 P-1 審查
- [x] 5.2 檢查「if-elif-else→流程圖」過場 (L231)：`畫流程圖可能聽起來很無聊，但它是你解決複雜判斷的秘密武器。` — 評估是否足夠
- [x] 5.3 檢查「流程圖→Judge 解題」過場 (L295-296)：目前只有 `---` 分隔，無過場文字。若確認缺少，補上 2~3 句銜接

## 6. F-1 圖片佔位符雙行格式

- [x] [P] 6.1 補齊圖 9（L25）的 `![📷 **圖 9**：...](/assets/tutor/py/ch1/圖九.png)` image link 行，放在 `> 📷` caption 行之前
- [x] [P] 6.2 補齊圖 10（L123）的 `![](path)` image link 行
- [x] [P] 6.3 補齊圖 11（L227）的 `![](path)` image link 行
- [x] [P] 6.4 補齊圖 12（L293）的 `![](path)` image link 行

## 7. K-1 顏文字語氣密度

- [x] 7.1 在 Judge 解題段落（L297-388）中插入至少 3 個情感標點元素（顏文字、括號笑話或學生對話），間距不少於 10 行。建議位置：L320 附近（IPO 分析完成後）、L350 附近（兩種寫法都 AC 後）、L380 附近（順序錯誤警告後）

## 8. S-2（H2 邊界）補充

- [x] 8.1 在 L231 的 H2 過場文字開頭加入回歸連接詞（例如「好啦，不要怕巢狀 if。」），連結回 L225 kaomoji 之前的巢狀 if 話題

## 9. 其餘規則掃描

- [x] 9.1 T-1 terminology forward-reference rule：確認 1-3 沒有使用模組二的概念（迴圈、串列、函式等）。特別注意 L46 `布林值（Boolean，bool）` 是否在概念動機之前就引入正式術語
- [x] 9.2 S-2 post-humor connector rule：掃描所有 kaomoji/笑話後的下一句，特別注意 L166 `ʅ（´◔౪◔）ʃ` 和 L84 `Σ(ﾟДﾟ；≡；ﾟдﾟ)` 後的銜接（H3 邊界豁免適用時標記為 pass）
- [x] 9.3 E-1 error prevention rule：確認 `==` vs `=` 混淆的警告 (L77-84) 出現在 `if` 語法首次出現的同一區塊內。另外在邏輯運算子引入處（L88-97）加入 `score >= 60 and <= 80` 常見錯誤的警告
- [x] 9.4 最終校讀全檔，確認 prose 語氣一致，無遺漏修改
